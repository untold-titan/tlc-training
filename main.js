document.querySelector('#app').innerHTML = `
  <div>
  </div>
`
//In a normal web application that is built in vanilla JS, and the pages are injected into the html programatically. This is what ReactJS does.
//If we want more functionality out of the site, this will be where it goes, or we switch to a JS framework, like Svelte